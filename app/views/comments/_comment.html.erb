<%= div_for comment do %>
	<p>
		<strong>
			Posted <%= time_ago_in_words(comment.created_at) %> ago
		</strong>
		<br/>
		<%= comment.body %>
	</p>
	<p>
		<%= link_to 'Remove comment', [comment.post, comment], :confirm => 'Are you sure?', :method => :delete %> 
	</p>
<% end %>